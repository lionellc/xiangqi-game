        // DOM元素
        const chessGrid = document.getElementById('chessGrid');
        const pieceSelector = document.getElementById('pieceSelector');
        const recordArea = document.getElementById('recordArea');
        const setupPanel = document.getElementById('setupPanel');
        const redClock = document.getElementById('redClock');
        const blackClock = document.getElementById('blackClock');
        const redStepTimeDisplay = document.getElementById('redStepTime');
        const blackStepTimeDisplay = document.getElementById('blackStepTime');
        const commentInput = document.getElementById('commentInput');
        const commentDisplay = document.getElementById('commentDisplay');
        const currentMoveInfo = document.getElementById('currentMoveInfo');
        const clockSettings = document.getElementById('clockSettings');
        const gameSelect = document.getElementById('gameSelect');
        const gameNameInput = document.getElementById('gameNameInput');
        const gameStatusInfo = document.getElementById('gameStatusInfo');
        const viewCurrentBtn = document.getElementById('viewCurrentBtn');
        const viewFragmentsBtn = document.getElementById('viewFragmentsBtn');
        const flipBoardBtn = document.getElementById('flipBoardBtn');
        const markBranchBtn = document.getElementById('markBranchBtn');
        const deleteBranchBtn = document.getElementById('deleteBranchBtn');
        const mirrorRecordBtn = document.getElementById('mirrorRecordBtn');
        const fileStatus = document.getElementById('fileStatus');
        const newFileBtn = document.getElementById('newFileBtn');
        const openFileBtn = document.getElementById('openFileBtn');
        const saveFileBtn = document.getElementById('saveFileBtn');
        const saveAsBtn = document.getElementById('saveAsBtn');
        const playBtn = document.getElementById('playBtn');
        const playFragmentsBtn = document.getElementById('playFragmentsBtn');
        const stopPlayBtn = document.getElementById('stopPlayBtn');
        const voiceToggle = document.getElementById('voiceToggle');
        const markUnderlineBtn = document.getElementById('markUnderlineBtn');
